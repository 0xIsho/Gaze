[/Engine/Net/Client]
ServerAddress            = "127.0.0.1"
ServerPort               = 54321 ; MUST match ListenPort in "Server.conf"
ConnectionAttemptTimeout = 3000  ; Milliseconds before the server is assumed unreachable in the initial connection attempt
